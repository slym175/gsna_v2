jQuery(document).ready(function () {
    // jQuery('.one-line').keyup(function(event) { 
    //     if (event.keyCode === 13) { 
    //         jQuery(this).val(jQuery(this).val() + "\r\n")
    //     } 
    // });
    // var subjects = jQuery.parseJSON(data.subjects);
    // console.log(JSON.stringify(subjects))
});